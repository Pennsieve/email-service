# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# IntelliJ
.idea

# Lambda build artifacts (bootstrap binary + deployment zip)
/bin

# Scala / sbt build artifacts (client-scala). project/build.properties is
# committed (pins the sbt version); target/ and .bsp are not.
target/
.bsp/
